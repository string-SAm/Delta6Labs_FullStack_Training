function App(
    name:string,
    a:number,
    b:number
    ){
        console.log(`Hello ${name}`);
        const sum=a+b
        console.log(`The sum of ${a} and ${b} is: ${sum}`);
    }

App('sam',3,4)


